{{h.form('')}}
    {{form.render()|safe}}

    <div class="actions">
        {{h.submit('submit', 'Salvar', class_='btn success')}}
        <input type="button" value="Cancelar" onclick="window.location='{{url_base|route_url}}'" class="btn danger">
    </div>
{{h.end_form()}}